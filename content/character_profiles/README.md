# Character visual profiles

This directory contains data-only visual/body profiles referenced by safe token id from `CharacterDefinition.visual_profile`.

Profiles may define validated palette and body measurements only. They must not contain executable scripts, arbitrary resource paths, or native/plugin code.
